<view class="container">
  <button class="loginwx-button" open-type="getUserInfo" bindgetuserinfo="onGetUserInfo">微信授权登录</button>
  <button class="loginbh-button" open-type="getUserInfo" bindgetuserinfo="onGetUserInfo">北航邮箱登录</button>


  <view wx:if="{{userInfo}}" class="welcome-section">
    <text class="welcome-text">欢迎, {{userInfo.nickName}}!</text>
    <image src="{{userInfo.avatarUrl}}" class="avatar-image"></image>
  </view>
</view>
